version: '3'

services:
  app:
    image: arimaratalinne/webacademy:latest
    container_name: webacademydemo
    restart: always
    ports:
      - "8081:8080"
    environment:
      NODE_ENV: production